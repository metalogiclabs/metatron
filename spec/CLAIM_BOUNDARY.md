# Claim Boundary

## Historical V0 qualified implementation claim

The earlier finite V0 qualified a complete toy lifecycle: exact obstruction,
verified extension, promotion, earned relation, query-relative compression,
restart, revocation and regrowth. Its identity remains recorded in
`evidence/qualified-runs/metatron-v0.json`.

## Minimal Warrant Graph V0 qualified implementation claim

A content-addressed append-only warrant graph can serve as the sole runtime
authority while preserving deterministic replay, dependency-sensitive live
closure, revocation without historical deletion, canonical persistence, and a
Lean-core reference semantics. The public runtime nucleus consists only of
`Node` and `WarrantGraph`; policy and domain-specific capability types are
outside the trusted semantics.

The compatibility bridge from the earlier V0 runtime is independently sealed in
`evidence/qualified-runs/graph-backed-v0-compat.json`.

## Not claimed

- unrestricted self-improvement;
- arbitrary ontology discovery;
- universal unique repair;
- GeneratedCongruence(Omega_earned) = PathBehEq;
- residualAdjoin_strictLyapunov;
- nonlinear support/recombination;
- full dependency-driven Flash reclosure across arbitrary external domains;
- the full (state, value, warrant, residual) execution interface;
- infinite productivity;
- cryptographic authentication of who may append or revoke a warrant.

## Status words

QUALIFIED, PARTIAL, PENDING, HISTORICAL, CONJECTURAL.
