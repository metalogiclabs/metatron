# G14-001 PProd Replication Contract V0

**Status:** preregistered replication boundary  
**Branch:** `metatron-g14-pprod-replication-v0`  
**Predecessor authority:** G13 exact head `6b50ae9d3a5543528ca815b69b423978dee0ab08`

G14 is not a feature-development licence. It is a replication experiment over
tutorial 041 / exact `PProd`.

The preregistered hypothesis is:

[
Delta_{041} =
{\text{Sort-polymorphic parameter/result levels}}.
]

G13 remains the executable ablation oracle. The scientific question is whether
this delta is sufficient while every previously sealed distinction remains
unchanged.

## Required causal premises

A completed G14 certificate must bind:

1. the exact externally sealed G13 predecessor;
2. the exact frozen tutorial-041 fixture hash;
3. the preregistered proposed delta above;
4. the observed structural delta after mechanical diff;
5. exact-head qualification evidence for the G14 candidate.

The G13 external seal is recorded as:

- run: `35668452215`
- job: `106559301866`
- artifact: `10670935477`
- digest: `sha256:fe0207e05ffb8564ce7ff076250f482b69a58bd4928fe436204c2d62c038f556`

## Qualification boundary

For a qualifying G14 candidate:

- tutorials 001-040 must be behaviorally identical to sealed G13;
- exact tutorial 041 / `PProd` must be ACCEPT;
- malformed claims inside the recognized PProd envelope must be REJECT;
- indexed, recursive, unsafe, nested, dependent, or otherwise broader
  parameterized neighbors must remain UNKNOWN;
- no existing G9-G13 handler may be broadened;
- no iota, projection, eta, general positivity, indexed-inductive, recursive-
  inductive, nested-inductive, or generic parameterized-inductive authority is
  earned.

The expected differential, if G14 qualifies, is therefore:

[
40\;\text{identical}
+1\;\text{earned delta}
+0\;\text{mismatches}.
]

## Replication, not promotion

A positive G14 result may establish that the G13 decomposition replicated. It
does **not** itself authorize a shared classifier or generic parameterized-
inductive abstraction.

The G14 evidence record must therefore state:

`shared_abstraction_promoted = false`

regardless of whether the hypothesis is replicated.

Any common architecture belongs to a later G15 promotion experiment whose
premises include separately sealed G12, G13, and G14 evidence.

## Formal warrant boundary

IdealLean may be extended only enough to express and prove the semantic
promotion law actually required by tutorial 041.

A completed G14 certificate must distinguish:

- semantic environment-validity warrant;
- Rust refinement: false;
- whole-checker verification: false.

## Performance boundary

Deterministic counters may be recorded. Hardware-counter performance promotion
requires a comparable cohort and measurement authority.

Absent that evidence, the certificate must retain a diagnostic/UNKNOWN
performance status and make no performance promotion claim.

## Metatron authority boundary

This contract changes no trusted runtime concept.

The Metatron nucleus remains:

```text
Node(kind, payload, premises)
append(Log, Node) -> Log
```

A completed G14 result is external evidence that may later be appended as an
ordinary warranted lineage node. The replication protocol itself is not runtime
authority.
