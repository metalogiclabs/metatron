# Metatron V0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Build the smallest complete Metatron lifecycle: exact finite obstruction → fresh verified capability → promotion and earned relation → query-relative compression → serialization/restart → revocation → query-driven regrowth, with Lean reference semantics and a differential Python runtime.

**Architecture:** V0 uses one deliberately tiny three-state fixture so the lifecycle is exhaustive and auditable. Lean defines reference semantics; a dependency-free Python runtime carries lineage, authority, projection, serialization, and regrowth; a differential test compares Python behavior against values computed by Lean. The general Nucleus universal-property branch remains PENDING and is never silently upgraded.

**Tech Stack:** Lean v4.35.0-rc2; Mathlib 44ba35c6daa9d69aff8fed9fff9bbde17ded774d; Lake; Python 3.12 standard library; unittest; JSON; GitHub Actions ubuntu-24.04.

**Spec:** docs/superpowers/specs/2026-09-22-metatron-v0-design.md

## Global Constraints

- Historical repos remain authoritative provenance.
- Residuals earn generators; warrant earns relations.
- Search exhaustion is not insufficiency: V0 uses exhaustive finite closure.
- GeneratedCongruence(Omega_earned) = PathBehEq is outside V0.
- residualAdjoin_strictLyapunov is outside V0.
- Nucleus Universal Property V0 stays PENDING until its own green gate.
- V0 fixture: states ZERO, ONE, TWO; id = (0,1,2); step = (1,1,2).
- Earned relation: step ; step = step, verified on all three states.
- IS_ZERO merges ONE/TWO; adding IS_ONE regrows three singleton classes.
- Lineage is append-only.
- Revocation disables active execution without deleting history.
- JSON serialization is deterministic.
- Python runtime uses standard library only.
- No sorry, admit, local axiom, constant, or unsafe under formal/Metatron/.
- Every runtime behavior starts with a failing unittest.
- Final qualification requires a fresh exact-head GitHub Actions success.

## Review Focus

1. Target already in closure must produce no residual.
2. Unverified relations must not install.
3. Non-congruent quotienting must fail closed.
4. Revoked capability must remain historical but not executable.
5. Restart must preserve every mutable machine field exactly.

---

### Task 1: Seed constitution and claim boundary

**Files:** README.md, spec/CONSTITUTION.md, spec/CLAIM_BOUNDARY.md

**Produces:** stable public vocabulary and status words.

- [ ] **Step 1: Create README.md**

~~~markdown
# Metatron

**Metatron is a proof-carrying developmental computer.**

Core rules:

- residuals earn generators;
- warrant earns relations;
- free completion supplies compositional consequence;
- quotient removes distinctions the protected future cannot expose;
- compilation makes the resulting presentation executable.

V0 is finite: exact obstruction → verified extension → promotion → reuse →
restart → query-relative compression → revocation → regrowth.
~~~

- [ ] **Step 2: Create spec/CONSTITUTION.md**

~~~markdown
# Metatron Constitution

1. Search exhaustion is not structural insufficiency.
2. Exact certified residuals may earn generators.
3. Warrant may earn equations or identifications.
4. Never add an equation merely because it is convenient.
5. Preserve every distinction a lawful protected future can expose.
6. Collapse everything else from the active presentation.
7. Keep lineage for warrant, not for execution.
8. Promotion requires independent verification.
9. Revocation changes authority, not historical occurrence.
10. When a new query exposes an old hidden distinction, regrow rather than guess.
11. Every constitutional abstraction needs a positive result, negative fixture, and ablation.
12. QUALIFIED means a fresh passing gate at an exact source head.
~~~

- [ ] **Step 3: Create spec/CLAIM_BOUNDARY.md**

~~~markdown
# Claim Boundary

## Intended V0 qualified claim

A finite proof-carrying developmental computer can certify an exact capability
obstruction, verify and promote a fresh capability, retain append-only lineage,
compress active state relative to protected queries, restart without semantic
drift, revoke authority without deleting history, and regrow when a later query
requires a previously hidden distinction.

## Not claimed

- unrestricted self-improvement;
- arbitrary ontology discovery;
- universal unique repair;
- GeneratedCongruence(Omega_earned) = PathBehEq;
- residualAdjoin_strictLyapunov;
- infinite productivity.

## Status words

QUALIFIED, PARTIAL, PENDING, HISTORICAL, CONJECTURAL.
~~~

- [ ] **Step 4: Verify and commit**

~~~bash
grep -F "residuals earn generators" README.md
grep -F "Search exhaustion is not structural insufficiency." spec/CONSTITUTION.md
grep -F "GeneratedCongruence(Omega_earned) = PathBehEq" spec/CLAIM_BOUNDARY.md
git add README.md spec
git commit -m "docs: seed Metatron constitution and claim boundary"
~~~

Expected: all checks exit 0.

### Task 2: Pin evidence and validate statuses

**Files:** evidence/manifests/dependencies.yaml, scripts/check_manifest.py, tests/test_manifest.py

**Produces:** load_manifest(path) and validate_manifest(data).

- [ ] **Step 1: Write failing manifest test**

~~~python
import unittest
from pathlib import Path
from scripts.check_manifest import load_manifest, validate_manifest

class ManifestTests(unittest.TestCase):
    def test_repo_manifest(self):
        self.assertEqual(
            validate_manifest(load_manifest(Path("evidence/manifests/dependencies.yaml"))),
            [],
        )

    def test_qualified_requires_exact_commit(self):
        bad = {"schema_version": 1, "dependencies": [
            {"id": "x", "status": "QUALIFIED", "repo": "a/b", "commit": "main"}
        ]}
        self.assertTrue(validate_manifest(bad))

if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run python -m unittest tests.test_manifest -v.

Expected: import failure for scripts.check_manifest.

- [ ] **Step 3: Implement validator**

~~~python
import json, re, sys
from pathlib import Path

STATUSES = {"QUALIFIED", "PARTIAL", "PENDING", "HISTORICAL", "CONJECTURAL"}
HEX40 = re.compile(r"^[0-9a-f]{40}$")

def load_manifest(path: Path) -> dict:
    return json.loads(path.read_text())

def validate_manifest(data: dict) -> list[str]:
    errors = []
    if data.get("schema_version") != 1:
        errors.append("schema_version must be 1")
    deps = data.get("dependencies")
    if not isinstance(deps, list):
        return errors + ["dependencies must be a list"]
    seen = set()
    for dep in deps:
        ident = dep.get("id", "<missing>")
        if ident in seen:
            errors.append(f"{ident}: duplicate id")
        seen.add(ident)
        if dep.get("status") not in STATUSES:
            errors.append(f"{ident}: invalid status")
        if dep.get("status") == "QUALIFIED" and not HEX40.fullmatch(dep.get("commit", "")):
            errors.append(f"{ident}: QUALIFIED requires exact commit")
        run = dep.get("qualification_run")
        if dep.get("status") == "QUALIFIED" and run and run.get("conclusion") != "success":
            errors.append(f"{ident}: qualified run must succeed")
    return errors

if __name__ == "__main__":
    errs = validate_manifest(load_manifest(Path("evidence/manifests/dependencies.yaml")))
    print("\n".join(errs))
    raise SystemExit(bool(errs))
~~~

- [ ] **Step 4: Create manifest**

Use JSON-compatible YAML with these exact dependency records:

~~~json
{
  "schema_version": 1,
  "dependencies": [
    {
      "id": "clc-nonlinear-lineage-v0",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "branch": "clc-nonlinear-lineage-v0",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "4158401de2cf047f912aa61db967393ad34cf061",
      "qualification_run": {
        "id": 35555539801,
        "conclusion": "success",
        "url": "https://github.com/heathsanchez/Minimal-Sufficient-Interface/actions/runs/35555539801"
      }
    },
    {
      "id": "typed-behavioural-congruence",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "6be7d423dd7683805d33d5789750625793fc7793"
    },
    {
      "id": "generated-stage",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "b8ce0d66072a325a660f1f4d0bc71e7f29fbf144"
    },
    {
      "id": "minimal-repair",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "de6fbc0289b48697da0f25cf97a1d6e09d70822b"
    },
    {
      "id": "nucleus-universal-property-v0",
      "status": "PENDING",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "branch": "nucleus-universal-property-v0",
      "commit": "c9b3c82b367d9612f94bea3ce5446ffc0728c35d",
      "last_observed_run": {
        "id": 35594301350,
        "conclusion": "failure"
      }
    }
  ]
}
~~~

- [ ] **Step 5: Verify and commit**

~~~bash
python -m unittest tests.test_manifest -v
python scripts/check_manifest.py
git add evidence scripts tests/test_manifest.py
git commit -m "feat(evidence): pin programme dependencies"
~~~

Expected: tests pass and checker exits 0.

### Task 3: Exact finite obstruction

**Files:** runtime/metatron/model.py, runtime/metatron/machine.py, runtime/__init__.py, runtime/metatron/__init__.py, tests/test_obstruction.py

**Produces:** State, Query, Table, ID_TABLE, STEP_TABLE, compose, Residual, closure, certify_no_resolution.

- [ ] **Step 1: Write failing tests**

~~~python
import unittest
from runtime.metatron.model import ID_TABLE, STEP_TABLE
from runtime.metatron.machine import closure, certify_no_resolution

class ObstructionTests(unittest.TestCase):
    def test_genesis_cannot_express_step(self):
        self.assertEqual(closure({ID_TABLE}), frozenset({ID_TABLE}))
        r = certify_no_resolution({ID_TABLE}, STEP_TABLE)
        self.assertIsNotNone(r)
        self.assertEqual(r.closure_size, 1)

    def test_existing_target_emits_no_residual(self):
        self.assertIsNone(certify_no_resolution({ID_TABLE}, ID_TABLE))

if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run python -m unittest tests.test_obstruction -v.

Expected: missing runtime module.

- [ ] **Step 3: Implement model**

~~~python
from dataclasses import dataclass
from enum import IntEnum, StrEnum
from hashlib import sha256
from typing import TypeAlias

class State(IntEnum):
    ZERO = 0
    ONE = 1
    TWO = 2

class Query(StrEnum):
    IS_ZERO = "is_zero"
    IS_ONE = "is_one"

Table: TypeAlias = tuple[int, int, int]
ID_TABLE: Table = (0, 1, 2)
STEP_TABLE: Table = (1, 1, 2)

def compose(first: Table, second: Table) -> Table:
    return tuple(second[first[i]] for i in range(3))

def table_digest(table: Table) -> str:
    return sha256(bytes(table)).hexdigest()

@dataclass(frozen=True)
class Residual:
    target: Table
    closure_size: int
    closure_digest: str
~~~

- [ ] **Step 4: Implement closure certificate**

~~~python
from hashlib import sha256
from .model import Residual, Table, compose

def closure(tables):
    known = set(tables)
    changed = True
    while changed:
        changed = False
        snapshot = tuple(known)
        for first in snapshot:
            for second in snapshot:
                candidate = compose(first, second)
                if candidate not in known:
                    known.add(candidate)
                    changed = True
    return frozenset(known)

def certify_no_resolution(active_tables, target):
    reachable = closure(active_tables)
    if target in reachable:
        return None
    payload = b"|".join(bytes(t) for t in sorted(reachable))
    return Residual(target, len(reachable), sha256(payload).hexdigest())
~~~

- [ ] **Step 5: Verify and commit**

Run python -m unittest tests.test_obstruction -v. Expected 2/2 PASS.

Commit: feat(runtime): certify exact finite capability obstruction

### Task 4: Verified generator and earned relation

**Files:** modify model.py and machine.py; create tests/test_promotion.py

**Produces:** Capability, Certificate, Relation, LineageEvent, Machine and promotion/relation APIs.

- [ ] **Step 1: Write failing test**

The test must:
- genesis;
- certify STEP_TABLE;
- free_extend as step;
- verify_candidate against STEP_TABLE;
- promote;
- execute step ZERO → ONE and TWO → TWO;
- reject install_relation before warrant;
- verify step ; step = step;
- install the verified relation.

- [ ] **Step 2: Verify RED**

Run python -m unittest tests.test_promotion -v.

Expected: Machine missing.

- [ ] **Step 3: Add records**

~~~python
@dataclass(frozen=True)
class Capability:
    name: str
    table: Table
    certificate_id: str | None = None

@dataclass(frozen=True)
class Certificate:
    id: str
    claim: str
    digest: str

@dataclass(frozen=True)
class Relation:
    left: tuple[str, ...]
    right: str

@dataclass(frozen=True)
class LineageEvent:
    kind: str
    payload: dict[str, object]
~~~

- [ ] **Step 4: Implement Machine**

Machine must:
- install id under live certificate genesis:id;
- append GENESIS, RESIDUAL, CANDIDATE, VERIFY, PROMOTE, VERIFY_RELATION, INSTALL_RELATION events;
- verify candidate by exact table equality;
- verify relation by exhaustive extensional table equality;
- reject unverified relation installation;
- execute only live, non-revoked capabilities.

Use SHA-256 for certificate digests.

- [ ] **Step 5: Verify and commit**

Run obstruction + promotion tests. Expected 4/4 PASS.

Commit: feat(runtime): promote verified generators and earned relations

### Task 5: Query-relative projection and regrowth

**Files:** modify model.py and machine.py; create tests/test_projection.py

**Produces:** eval_query, partition_for_queries, project_table, NonCongruentProjectionError, Machine.set_queries, Machine.project.

- [ ] **Step 1: Write failing tests**

Tests must prove:
- IS_ZERO projects to ((0,), (1,2));
- adding IS_ONE regrows ((0,), (1,), (2,));
- pre-regrowth lineage is an unchanged prefix;
- project_table((0,2,2), ((0,1),(2,))) raises NonCongruentProjectionError;
- STEP_TABLE descends on IS_ZERO quotient to (1,1).

- [ ] **Step 2: Verify RED**

Run python -m unittest tests.test_projection -v.

- [ ] **Step 3: Implement eval_query**

~~~python
def eval_query(query: Query, state: State) -> bool:
    if query == Query.IS_ZERO:
        return state == State.ZERO
    if query == Query.IS_ONE:
        return state == State.ONE
    raise AssertionError(query)
~~~

- [ ] **Step 4: Implement projection**

partition_for_queries groups by complete query signature and sorts blocks by minimum member. project_table requires every source block to land in exactly one target block. Machine.project checks every active capability before changing active_partition and appends PROJECT or REGROW.

- [ ] **Step 5: Verify and commit**

Run Tasks 3–5 tests. Expected 7/7 PASS.

Commit: feat(runtime): add query-relative projection and regrowth

### Task 6: Revocation without historical deletion

**Files:** modify machine.py; create tests/test_revocation.py

**Produces:** RevokedCapabilityError and Machine.revoke.

- [ ] **Step 1: Write failing test**

Promote step, save its PROMOTE event, revoke its certificate, require:
- execute step raises RevokedCapabilityError;
- step is absent from active capability names;
- saved PROMOTE event remains;
- REVOKE event exists.

- [ ] **Step 2: Verify RED**

Run python -m unittest tests.test_revocation -v.

- [ ] **Step 3: Implement**

revoke rejects unknown certificates, adds to revoked_certificates, appends REVOKE, and never deletes capability or lineage records. execute checks revoked certificates explicitly.

- [ ] **Step 4: Verify and commit**

Run all runtime tests through Task 6. Expected 8/8 PASS.

Commit: feat(runtime): make authority revocation history-preserving

