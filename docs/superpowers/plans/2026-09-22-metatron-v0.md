# Metatron V0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Build the smallest complete Metatron lifecycle: exact finite obstruction → fresh verified capability → promotion and earned relation → query-relative compression → serialization/restart → revocation → query-driven regrowth, with Lean reference semantics and a differential Python runtime.

**Architecture:** V0 uses one deliberately tiny three-state fixture so the lifecycle is exhaustive and auditable. Lean defines reference semantics; a dependency-free Python runtime carries lineage, authority, projection, serialization, and regrowth; a differential test compares Python behavior against values computed by Lean. The general Nucleus universal-property branch remains PENDING and is never silently upgraded.

**Tech Stack:** Lean v4.35.0-rc2; Mathlib 44ba35c6daa9d69aff8fed9fff9bbde17ded774d; Lake; Python 3.12 standard library; unittest; JSON; GitHub Actions ubuntu-24.04.

**Spec:** docs/superpowers/specs/2026-09-22-metatron-v0-design.md

## Global Constraints

- Historical repos remain authoritative provenance.
- Residuals earn generators; warrant earns relations.
- Search exhaustion is not insufficiency: V0 uses exhaustive finite closure.
- GeneratedCongruence(Omega_earned) = PathBehEq is outside V0.
- residualAdjoin_strictLyapunov is outside V0.
- Nucleus Universal Property V0 stays PENDING until its own green gate.
- V0 fixture: states ZERO, ONE, TWO; id = (0,1,2); step = (1,1,2).
- Earned relation: step ; step = step, verified on all three states.
- IS_ZERO merges ONE/TWO; adding IS_ONE regrows three singleton classes.
- Lineage is append-only.
- Revocation disables active execution without deleting history.
- JSON serialization is deterministic.
- Python runtime uses standard library only.
- No sorry, admit, local axiom, constant, or unsafe under formal/Metatron/.
- Every runtime behavior starts with a failing unittest.
- Residual growth requires an issued residual certificate persisted by the machine; recomputing the same fields is not sufficient.
- Every supplied certificate object must equal the certificate persisted under the same ID before its kind/subject is trusted.
- Candidate verification derives its target from the persisted residual certificate/frozen contract; callers do not supply an expected table.
- V0 explicitly defers nonlinear support/recombination, dependency-driven Flash reclosure, and the full (state, value, warrant, residual) execution interface.
- Final qualification distinguishes a qualified implementation head from the later evidence-closure head to avoid impossible self-reference.
- Final qualification requires a fresh exact-head GitHub Actions success.

## Review Focus

1. Target already in closure must produce no residual, and a forged/stale residual certificate must not authorize growth.
2. Supplied certificate objects must exactly match their persisted store entries; forged objects reusing a live ID must fail.
3. Unverified relations and non-congruent quotients must fail closed.
4. Revoked capability must remain historical but not executable.
5. Restart must preserve every mutable machine field, including capability and residual certificate stores, exactly.

---

### Task 1: Seed constitution and claim boundary

**Files:** README.md, spec/CONSTITUTION.md, spec/CLAIM_BOUNDARY.md

**Produces:** stable public vocabulary and status words.

- [ ] **Step 1: Create README.md**

~~~markdown
# Metatron

**Metatron is a proof-carrying developmental computer.**

Core rules:

- residuals earn generators;
- warrant earns relations;
- free completion supplies compositional consequence;
- quotient removes distinctions the protected future cannot expose;
- compilation makes the resulting presentation executable.

V0 is finite: exact obstruction → verified extension → promotion → reuse →
restart → query-relative compression → revocation → regrowth.
~~~

- [ ] **Step 2: Create spec/CONSTITUTION.md**

~~~markdown
# Metatron Constitution

1. Search exhaustion is not structural insufficiency.
2. Exact certified residuals may earn generators.
3. Warrant may earn equations or identifications.
4. Never add an equation merely because it is convenient.
5. Preserve every distinction a lawful protected future can expose.
6. Collapse everything else from the active presentation.
7. Keep lineage for warrant, not for execution.
8. Promotion requires independent verification.
9. Revocation changes authority, not historical occurrence.
10. When a new query exposes an old hidden distinction, regrow rather than guess.
11. Every constitutional abstraction needs a positive result, negative fixture, and ablation.
12. QUALIFIED means a fresh passing gate at an exact source head.
~~~

- [ ] **Step 3: Create spec/CLAIM_BOUNDARY.md**

~~~markdown
# Claim Boundary

## Intended V0 qualified claim

A finite proof-carrying developmental computer can certify an exact capability
obstruction, verify and promote a fresh capability, retain append-only lineage,
compress active state relative to protected queries, restart without semantic
drift, revoke authority without deleting history, and regrow when a later query
requires a previously hidden distinction.

## Not claimed

- unrestricted self-improvement;
- arbitrary ontology discovery;
- universal unique repair;
- GeneratedCongruence(Omega_earned) = PathBehEq;
- residualAdjoin_strictLyapunov;
- nonlinear support/recombination;
- dependency-driven Flash reclosure;
- the full (state, value, warrant, residual) execution interface;
- infinite productivity.

## Status words

QUALIFIED, PARTIAL, PENDING, HISTORICAL, CONJECTURAL.
~~~

- [ ] **Step 4: Verify and commit**

~~~bash
grep -F "residuals earn generators" README.md
grep -F "Search exhaustion is not structural insufficiency." spec/CONSTITUTION.md
grep -F "GeneratedCongruence(Omega_earned) = PathBehEq" spec/CLAIM_BOUNDARY.md
git add README.md spec
git commit -m "docs: seed Metatron constitution and claim boundary"
~~~

Expected: all checks exit 0.

### Task 2: Pin evidence and validate statuses

**Files:** evidence/manifests/dependencies.yaml, scripts/check_manifest.py, tests/test_manifest.py

**Produces:** load_manifest(path) and validate_manifest(data).

- [ ] **Step 1: Write failing manifest test**

~~~python
import unittest
from pathlib import Path
from scripts.check_manifest import load_manifest, validate_manifest

class ManifestTests(unittest.TestCase):
    def test_repo_manifest(self):
        self.assertEqual(
            validate_manifest(load_manifest(Path("evidence/manifests/dependencies.yaml"))),
            [],
        )

    def test_qualified_requires_exact_commit_and_evidence(self):
        base = {
            "id": "x",
            "status": "QUALIFIED",
            "repo": "a/b",
            "commit": "c" * 40,
            "blob_sha": "b" * 40,
            "qualification_run": {
                "id": 1,
                "conclusion": "success",
                "url": "https://example.invalid/run/1",
            },
        }
        self.assertEqual(
            validate_manifest({"schema_version": 1, "dependencies": [base]}),
            [],
        )

        for field in ("commit", "blob_sha", "qualification_run"):
            bad = dict(base)
            bad.pop(field)
            self.assertTrue(
                validate_manifest({"schema_version": 1, "dependencies": [bad]}),
                field,
            )

        bad_run = dict(base)
        bad_run["qualification_run"] = {"id": 1, "conclusion": "success"}
        self.assertTrue(
            validate_manifest({"schema_version": 1, "dependencies": [bad_run]}),
            "qualification_run.url",
        )

if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run python -m unittest tests.test_manifest -v.

Expected: import failure for scripts.check_manifest.

- [ ] **Step 3: Implement validator**

~~~python
import json, re, sys
from pathlib import Path

STATUSES = {"QUALIFIED", "PARTIAL", "PENDING", "HISTORICAL", "CONJECTURAL"}
HEX40 = re.compile(r"^[0-9a-f]{40}$")

def load_manifest(path: Path) -> dict:
    return json.loads(path.read_text())

def validate_manifest(data: dict) -> list[str]:
    errors = []
    if data.get("schema_version") != 1:
        errors.append("schema_version must be 1")
    deps = data.get("dependencies")
    if not isinstance(deps, list):
        return errors + ["dependencies must be a list"]
    seen = set()
    for dep in deps:
        ident = dep.get("id", "<missing>")
        if ident in seen:
            errors.append(f"{ident}: duplicate id")
        seen.add(ident)
        if dep.get("status") not in STATUSES:
            errors.append(f"{ident}: invalid status")
        if dep.get("status") == "QUALIFIED":
            if not HEX40.fullmatch(dep.get("commit", "")):
                errors.append(f"{ident}: QUALIFIED requires exact commit")
            blob_sha = dep.get("blob_sha")
            artifact_id = dep.get("artifact_id")
            if not (
                (isinstance(blob_sha, str) and HEX40.fullmatch(blob_sha))
                or isinstance(artifact_id, int)
            ):
                errors.append(f"{ident}: QUALIFIED requires exact blob_sha or artifact_id")
            run = dep.get("qualification_run")
            if not isinstance(run, dict):
                errors.append(f"{ident}: QUALIFIED requires qualification_run")
            else:
                if run.get("conclusion") != "success":
                    errors.append(f"{ident}: qualified run must succeed")
                if not isinstance(run.get("url"), str) or not run["url"]:
                    errors.append(f"{ident}: qualification_run requires URL")
    return errors

if __name__ == "__main__":
    errs = validate_manifest(load_manifest(Path("evidence/manifests/dependencies.yaml")))
    print("\n".join(errs))
    raise SystemExit(bool(errs))
~~~

- [ ] **Step 4: Create manifest**

Use JSON-compatible YAML with these exact dependency records:

~~~json
{
  "schema_version": 1,
  "dependencies": [
    {
      "id": "clc-nonlinear-lineage-v0",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "branch": "clc-nonlinear-lineage-v0",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "4158401de2cf047f912aa61db967393ad34cf061",
      "qualification_run": {
        "id": 35555539801,
        "conclusion": "success",
        "url": "https://github.com/heathsanchez/Minimal-Sufficient-Interface/actions/runs/35555539801"
      }
    },
    {
      "id": "typed-behavioural-congruence",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "6be7d423dd7683805d33d5789750625793fc7793",
      "qualification_run": {
        "id": 35555539761,
        "conclusion": "success",
        "url": "https://github.com/heathsanchez/Minimal-Sufficient-Interface/actions/runs/35555539761"
      }
    },
    {
      "id": "developmental-category",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "8e73b2bd3025b52b576159984c507312c111919b",
      "qualification_run": {
        "id": 35555539761,
        "conclusion": "success",
        "url": "https://github.com/heathsanchez/Minimal-Sufficient-Interface/actions/runs/35555539761"
      }
    },
    {
      "id": "generated-stage",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "b8ce0d66072a325a660f1f4d0bc71e7f29fbf144",
      "qualification_run": {
        "id": 35555539761,
        "conclusion": "success",
        "url": "https://github.com/heathsanchez/Minimal-Sufficient-Interface/actions/runs/35555539761"
      }
    },
    {
      "id": "minimal-repair",
      "status": "QUALIFIED",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "commit": "900a73c1d386ee0eee1205a1cf31d270f3311ef7",
      "blob_sha": "de6fbc0289b48697da0f25cf97a1d6e09d70822b",
      "qualification_run": {
        "id": 35555539761,
        "conclusion": "success",
        "url": "https://github.com/heathsanchez/Minimal-Sufficient-Interface/actions/runs/35555539761"
      }
    },
    {
      "id": "nucleus-universal-property-v0",
      "status": "PENDING",
      "repo": "heathsanchez/Minimal-Sufficient-Interface",
      "branch": "nucleus-universal-property-v0",
      "commit": "c9b3c82b367d9612f94bea3ce5446ffc0728c35d",
      "last_observed_run": {
        "id": 35594301350,
        "conclusion": "failure"
      }
    }
  ]
}
~~~

- [ ] **Step 5: Verify and commit**

~~~bash
python -m unittest tests.test_manifest -v
python scripts/check_manifest.py
git add evidence scripts tests/test_manifest.py
git commit -m "feat(evidence): pin programme dependencies"
~~~

Expected: tests pass and checker exits 0.

### Task 3: Exact finite obstruction

**Files:** runtime/metatron/model.py, runtime/metatron/machine.py, runtime/__init__.py, runtime/metatron/__init__.py, tests/test_obstruction.py

**Produces:** State, Query, Table, ID_TABLE, STEP_TABLE, compose, Residual, authority_digest, closure, and certify_no_resolution.

- [ ] **Step 1: Write failing tests**

~~~python
import unittest
from runtime.metatron.model import ID_TABLE, STEP_TABLE
from runtime.metatron.machine import authority_digest, closure, certify_no_resolution

class ObstructionTests(unittest.TestCase):
    def test_genesis_cannot_express_step(self):
        self.assertEqual(closure({ID_TABLE}), frozenset({ID_TABLE}))
        residual = certify_no_resolution({ID_TABLE}, STEP_TABLE)
        self.assertIsNotNone(residual)
        self.assertEqual(residual.closure_size, 1)
        self.assertEqual(residual.authority_digest, authority_digest({ID_TABLE}))

    def test_existing_target_emits_no_residual(self):
        self.assertIsNone(certify_no_resolution({ID_TABLE}, ID_TABLE))

if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run:

~~~bash
python -m unittest tests.test_obstruction -v
~~~

Expected: FAIL because runtime.metatron.model is missing.

- [ ] **Step 3: Create package markers and model**

Create runtime/__init__.py as an empty file.

Create runtime/metatron/__init__.py:

~~~python
from .model import ID_TABLE, STEP_TABLE, Query, Residual, State, Table

__all__ = [
    "ID_TABLE",
    "STEP_TABLE",
    "Query",
    "Residual",
    "State",
    "Table",
]
~~~

Create runtime/metatron/model.py:

~~~python
from dataclasses import dataclass
from enum import IntEnum, StrEnum
from hashlib import sha256
from typing import TypeAlias

class State(IntEnum):
    ZERO = 0
    ONE = 1
    TWO = 2

class Query(StrEnum):
    IS_ZERO = "is_zero"
    IS_ONE = "is_one"

Table: TypeAlias = tuple[int, int, int]
ID_TABLE: Table = (0, 1, 2)
STEP_TABLE: Table = (1, 1, 2)

def compose(first: Table, second: Table) -> Table:
    return tuple(second[first[i]] for i in range(3))

def table_digest(table: Table) -> str:
    return sha256(bytes(table)).hexdigest()

@dataclass(frozen=True)
class Residual:
    target: Table
    closure_size: int
    closure_digest: str
    authority_digest: str
~~~

- [ ] **Step 4: Implement exact closure and live-capability digest**

Create runtime/metatron/machine.py:

~~~python
from hashlib import sha256

from .model import Residual, Table, compose

def closure(tables) -> frozenset[Table]:
    known = set(tables)
    changed = True
    while changed:
        changed = False
        snapshot = tuple(known)
        for first in snapshot:
            for second in snapshot:
                candidate = compose(first, second)
                if candidate not in known:
                    known.add(candidate)
                    changed = True
    return frozenset(known)

def _tables_digest(tables) -> str:
    payload = b"|".join(bytes(table) for table in sorted(tables))
    return sha256(payload).hexdigest()

def authority_digest(active_tables) -> str:
    return _tables_digest(frozenset(active_tables))

def certify_no_resolution(active_tables, target):
    active = frozenset(active_tables)
    reachable = closure(active)
    if target in reachable:
        return None
    return Residual(
        target=target,
        closure_size=len(reachable),
        closure_digest=_tables_digest(reachable),
        authority_digest=authority_digest(active),
    )
~~~

- [ ] **Step 5: Verify GREEN**

Run:

~~~bash
python -m unittest tests.test_obstruction -v
~~~

Expected: 2/2 PASS.

- [ ] **Step 6: Commit**

~~~bash
git add runtime tests/test_obstruction.py
git commit -m "feat(runtime): certify exact finite capability obstruction"
~~~

### Task 4: Persisted residual warrants, verified generator, and earned relation

**Files:** modify runtime/metatron/model.py and runtime/metatron/machine.py; create tests/test_promotion.py

**Produces:** ResidualCertificate, Capability, Certificate, Relation, LineageEvent, Machine, UncertifiedResidualError, UnverifiedCertificateError, UnverifiedRelationError, UnknownCapabilityError, and proof-carrying promotion/relation APIs.

- [ ] **Step 1: Write failing tests**

~~~python
import unittest
from dataclasses import replace

from runtime.metatron.model import STEP_TABLE, ResidualCertificate, State
from runtime.metatron.machine import (
    Machine,
    UncertifiedResidualError,
    UnverifiedCertificateError,
    UnverifiedRelationError,
)


class PromotionTests(unittest.TestCase):
    def test_issued_residual_is_persisted_and_binds_candidate_verification(self):
        machine = Machine.genesis()
        residual_cert = machine.certify_target(STEP_TABLE)
        self.assertIsNotNone(residual_cert)
        self.assertEqual(
            machine.residual_certificates[residual_cert.id],
            residual_cert,
        )

        candidate = machine.free_extend(residual_cert, "step")
        self.assertEqual(candidate.residual_certificate_id, residual_cert.id)

        cert = machine.verify_candidate(candidate)
        machine.promote(candidate, cert)
        self.assertEqual(machine.execute("step", State.ZERO), State.ONE)
        self.assertEqual(machine.execute("step", State.TWO), State.TWO)

    def test_forged_or_stale_residual_cannot_force_growth(self):
        machine = Machine.genesis()
        issued = machine.certify_target(STEP_TABLE)

        forged = replace(issued, id="residual:forged")
        with self.assertRaises(UncertifiedResidualError):
            machine.free_extend(forged, "step")

        candidate = machine.free_extend(issued, "step")
        cert = machine.verify_candidate(candidate)
        machine.promote(candidate, cert)

        with self.assertRaises(UncertifiedResidualError):
            machine.free_extend(issued, "step-again")

    def test_forged_certificate_object_reusing_live_id_is_rejected(self):
        machine = Machine.genesis()
        residual_cert = machine.certify_target(STEP_TABLE)
        candidate = machine.free_extend(residual_cert, "step")
        cert = machine.verify_candidate(candidate)

        forged = replace(cert, subject_digest="0" * 64)
        with self.assertRaises(UnverifiedCertificateError):
            machine.promote(candidate, forged)

        machine.promote(candidate, cert)
        relation, relation_cert = machine.verify_relation(("step", "step"), "step")
        forged_relation_cert = replace(relation_cert, kind="capability")
        with self.assertRaises(UnverifiedRelationError):
            machine.install_relation(relation, forged_relation_cert)

    def test_relation_requires_its_own_persisted_warrant(self):
        machine = Machine.genesis()
        residual_cert = machine.certify_target(STEP_TABLE)
        candidate = machine.free_extend(residual_cert, "step")
        capability_cert = machine.verify_candidate(candidate)
        machine.promote(candidate, capability_cert)

        proposal = machine.propose_relation(("step", "step"), "step")
        with self.assertRaises(UnverifiedRelationError):
            machine.install_relation(proposal, None)
        with self.assertRaises(UnverifiedRelationError):
            machine.install_relation(proposal, capability_cert)

        relation, relation_cert = machine.verify_relation(("step", "step"), "step")
        machine.install_relation(relation, relation_cert)
        self.assertEqual(machine.relations(), ((("step", "step"), "step"),))


if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run:

~~~bash
python -m unittest tests.test_promotion -v
~~~

Expected: FAIL because Machine and persisted warrant types are missing.

- [ ] **Step 3: Add proof-carrying model records**

Append to runtime/metatron/model.py:

~~~python
@dataclass(frozen=True)
class ResidualCertificate:
    id: str
    target: Table
    target_digest: str
    closure_size: int
    closure_digest: str
    authority_digest: str


@dataclass(frozen=True)
class Capability:
    name: str
    table: Table
    residual_certificate_id: str | None = None
    certificate_id: str | None = None


@dataclass(frozen=True)
class Certificate:
    id: str
    kind: str
    subject_digest: str


@dataclass(frozen=True)
class Relation:
    left: tuple[str, ...]
    right: str


@dataclass(frozen=True)
class LineageEvent:
    kind: str
    payload: dict[str, object]
~~~

- [ ] **Step 4: Add exact persisted-warrant Machine implementation**

Keep Task 3 closure, authority_digest, and certify_no_resolution. Add imports:

~~~python
from dataclasses import replace

from .model import (
    ID_TABLE,
    Capability,
    Certificate,
    LineageEvent,
    Relation,
    ResidualCertificate,
    State,
    table_digest,
)
~~~

Add errors and digest helper:

~~~python
class UncertifiedResidualError(RuntimeError):
    pass

class UnverifiedCertificateError(RuntimeError):
    pass

class UnverifiedRelationError(RuntimeError):
    pass

class UnknownCapabilityError(KeyError):
    pass

def relation_digest(left, right, table) -> str:
    payload = repr((tuple(left), right, tuple(table))).encode()
    return sha256(payload).hexdigest()
~~~

Implement Machine:

~~~python
class Machine:
    def __init__(self) -> None:
        self.lineage = []
        self.capabilities = {}
        self.certificates = {}
        self.residual_certificates = {}
        self.live_certificates = set()
        self.revoked_certificates = set()
        self._relations = []
        self._next_residual_serial = 1

    @classmethod
    def genesis(cls):
        machine = cls()
        cert = Certificate(
            id="genesis:id",
            kind="capability",
            subject_digest=table_digest(ID_TABLE),
        )
        machine.certificates[cert.id] = cert
        machine.live_certificates.add(cert.id)
        machine.capabilities["id"] = Capability(
            "id", ID_TABLE, residual_certificate_id=None, certificate_id=cert.id
        )
        machine.lineage.append(LineageEvent("GENESIS", {"capability": "id"}))
        return machine

    def active_capability_names(self):
        return tuple(sorted(
            name
            for name, cap in self.capabilities.items()
            if cap.certificate_id in self.live_certificates
            and cap.certificate_id not in self.revoked_certificates
        ))

    def active_tables(self):
        return {self.capabilities[name].table for name in self.active_capability_names()}

    def certify_target(self, target):
        residual = certify_no_resolution(self.active_tables(), target)
        if residual is None:
            return None
        serial = self._next_residual_serial
        self._next_residual_serial += 1
        cert = ResidualCertificate(
            id=f"residual:{serial}:{table_digest(target)[:16]}",
            target=target,
            target_digest=table_digest(target),
            closure_size=residual.closure_size,
            closure_digest=residual.closure_digest,
            authority_digest=residual.authority_digest,
        )
        self.residual_certificates[cert.id] = cert
        self.lineage.append(LineageEvent(
            "RESIDUAL",
            {
                "certificate": cert.id,
                "target": list(target),
                "closure_size": cert.closure_size,
                "closure_digest": cert.closure_digest,
                "authority_digest": cert.authority_digest,
            },
        ))
        return cert

    def free_extend(self, residual_cert, name):
        stored = self.residual_certificates.get(residual_cert.id)
        if stored is None or stored != residual_cert:
            raise UncertifiedResidualError("residual certificate was not issued by this machine")
        current = certify_no_resolution(self.active_tables(), stored.target)
        if current is None:
            raise UncertifiedResidualError("residual is no longer obstructed")
        if (
            current.closure_digest != stored.closure_digest
            or current.authority_digest != stored.authority_digest
            or table_digest(stored.target) != stored.target_digest
        ):
            raise UncertifiedResidualError("residual certificate is stale")
        candidate = Capability(
            name=name,
            table=stored.target,
            residual_certificate_id=stored.id,
            certificate_id=None,
        )
        self.lineage.append(LineageEvent(
            "CANDIDATE",
            {"name": name, "residual_certificate": stored.id},
        ))
        return candidate

    def verify_candidate(self, candidate):
        residual_id = candidate.residual_certificate_id
        stored_residual = self.residual_certificates.get(residual_id)
        if stored_residual is None:
            raise UncertifiedResidualError("candidate lacks an issued residual")
        if table_digest(candidate.table) != stored_residual.target_digest:
            raise UnverifiedCertificateError("candidate does not match frozen residual target")
        cert = Certificate(
            id=f"cert:{candidate.name}:{table_digest(candidate.table)[:16]}",
            kind="capability",
            subject_digest=table_digest(candidate.table),
        )
        self.certificates[cert.id] = cert
        self.live_certificates.add(cert.id)
        self.lineage.append(LineageEvent(
            "VERIFY",
            {
                "certificate": cert.id,
                "capability": candidate.name,
                "residual_certificate": stored_residual.id,
            },
        ))
        return cert

    def _require_stored_certificate(self, certificate):
        stored = self.certificates.get(certificate.id)
        if stored is None or stored != certificate:
            raise UnverifiedCertificateError(
                "certificate object does not match persisted warrant"
            )
        return stored

    def promote(self, candidate, certificate):
        stored = self._require_stored_certificate(certificate)
        if stored.id not in self.live_certificates:
            raise UnverifiedCertificateError("certificate is not live")
        if stored.kind != "capability":
            raise UnverifiedCertificateError("certificate is not a capability warrant")
        if stored.subject_digest != table_digest(candidate.table):
            raise UnverifiedCertificateError("certificate does not warrant candidate")
        self.capabilities[candidate.name] = replace(
            candidate, certificate_id=stored.id
        )
        self.lineage.append(LineageEvent(
            "PROMOTE",
            {"capability": candidate.name, "certificate": stored.id},
        ))

    def _compose_names(self, names):
        table = ID_TABLE
        for name in names:
            try:
                capability = self.capabilities[name]
            except KeyError as exc:
                raise UnknownCapabilityError(name) from exc
            table = compose(table, capability.table)
        return table

    def propose_relation(self, left, right):
        return Relation(left=tuple(left), right=right)

    def verify_relation(self, left, right):
        relation = Relation(left=tuple(left), right=right)
        left_table = self._compose_names(relation.left)
        right_table = self._compose_names((right,))
        if left_table != right_table:
            raise ValueError("relation is not extensionally valid")
        digest = relation_digest(relation.left, relation.right, left_table)
        cert = Certificate(
            id=f"cert:relation:{digest[:16]}",
            kind="relation",
            subject_digest=digest,
        )
        self.certificates[cert.id] = cert
        self.live_certificates.add(cert.id)
        self.lineage.append(LineageEvent(
            "VERIFY_RELATION",
            {"left": list(relation.left), "right": right, "certificate": cert.id},
        ))
        return relation, cert

    def install_relation(self, relation, certificate):
        if certificate is None:
            raise UnverifiedRelationError("relation lacks warrant")
        try:
            stored = self._require_stored_certificate(certificate)
        except UnverifiedCertificateError as exc:
            raise UnverifiedRelationError(str(exc)) from exc
        left_table = self._compose_names(relation.left)
        expected = relation_digest(relation.left, relation.right, left_table)
        if (
            stored.id not in self.live_certificates
            or stored.kind != "relation"
            or stored.subject_digest != expected
        ):
            raise UnverifiedRelationError("certificate does not warrant this relation")
        self._relations.append((relation, stored.id))
        self.lineage.append(LineageEvent(
            "INSTALL_RELATION",
            {"left": list(relation.left), "right": relation.right, "certificate": stored.id},
        ))

    def relations(self):
        return tuple((relation.left, relation.right) for relation, _ in self._relations)

    def execute(self, name, state):
        try:
            capability = self.capabilities[name]
        except KeyError as exc:
            raise UnknownCapabilityError(name) from exc
        if capability.certificate_id not in self.live_certificates:
            raise UnverifiedCertificateError("capability certificate is not live")
        if capability.certificate_id in self.revoked_certificates:
            raise ValueError("capability certificate is revoked")
        return State(capability.table[int(state)])
~~~

- [ ] **Step 5: Verify GREEN**

Run:

~~~bash
python -m unittest tests.test_obstruction tests.test_promotion -v
~~~

Expected: 6/6 PASS.

- [ ] **Step 6: Commit**

~~~bash
git add runtime/metatron tests/test_promotion.py
git commit -m "feat(runtime): persist residual and verifier warrants"
~~~

### Task 5: Query-relative projection and regrowth

**Files:** modify runtime/metatron/model.py and runtime/metatron/machine.py; create tests/test_projection.py

**Produces:** eval_query, partition_for_queries, project_table, NonCongruentProjectionError, Machine.set_queries, Machine.project, Machine.active_partition.

- [ ] **Step 1: Write failing tests**

~~~python
import unittest

from runtime.metatron.model import Query, STEP_TABLE
from runtime.metatron.machine import (
    Machine,
    NonCongruentProjectionError,
    partition_for_queries,
    project_table,
)


class ProjectionTests(unittest.TestCase):
    def test_is_zero_compresses_then_is_one_regrows(self):
        machine = Machine.genesis()
        machine.set_queries([Query.IS_ZERO])
        machine.project()
        self.assertEqual(machine.active_partition, ((0,), (1, 2)))

        prefix = tuple(machine.lineage)
        machine.set_queries([Query.IS_ZERO, Query.IS_ONE])
        machine.project()
        self.assertEqual(machine.active_partition, ((0,), (1,), (2,)))
        self.assertEqual(tuple(machine.lineage[:len(prefix)]), prefix)

    def test_non_congruent_projection_fails_closed(self):
        with self.assertRaises(NonCongruentProjectionError):
            project_table((0, 2, 2), ((0, 1), (2,)))

    def test_step_descends_to_is_zero_quotient(self):
        partition = partition_for_queries([Query.IS_ZERO])
        self.assertEqual(project_table(STEP_TABLE, partition), (1, 1))


if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run:

~~~bash
python -m unittest tests.test_projection -v
~~~

Expected: FAIL because projection functions are missing.

- [ ] **Step 3: Implement eval_query**

Append to runtime/metatron/model.py:

~~~python
def eval_query(query: Query, state: State) -> bool:
    if query == Query.IS_ZERO:
        return state == State.ZERO
    if query == Query.IS_ONE:
        return state == State.ONE
    raise AssertionError(query)
~~~

- [ ] **Step 4: Implement deterministic projection**

Add to runtime/metatron/machine.py imports:

~~~python
from .model import Query, eval_query
~~~

Then add:

~~~python
class NonCongruentProjectionError(RuntimeError):
    pass


def partition_for_queries(
    queries: list[Query] | tuple[Query, ...],
) -> tuple[tuple[int, ...], ...]:
    groups: dict[tuple[bool, ...], list[int]] = {}
    for state in State:
        signature = tuple(eval_query(query, state) for query in queries)
        groups.setdefault(signature, []).append(int(state))
    return tuple(
        tuple(block)
        for block in sorted(groups.values(), key=lambda xs: xs[0])
    )


def project_table(
    table: Table,
    partition: tuple[tuple[int, ...], ...],
) -> tuple[int, ...]:
    class_of = {
        member: index
        for index, block in enumerate(partition)
        for member in block
    }
    projected = []
    for block in partition:
        targets = {class_of[table[source]] for source in block}
        if len(targets) != 1:
            raise NonCongruentProjectionError(
                f"table {table} splits active class {block}"
            )
        projected.append(next(iter(targets)))
    return tuple(projected)
~~~

Add to Machine.__init__:

~~~python
self.queries: list[Query] = []
self.active_partition: tuple[tuple[int, ...], ...] = ((0,), (1,), (2,))
~~~

Add methods:

~~~python
def set_queries(self, queries: list[Query]) -> None:
    self.queries = list(dict.fromkeys(queries))
    self.lineage.append(LineageEvent(
        "SET_QUERIES",
        {"queries": [query.value for query in self.queries]},
    ))

def project(self) -> None:
    old = self.active_partition
    new = partition_for_queries(self.queries)
    for name in self.active_capability_names():
        project_table(self.capabilities[name].table, new)
    self.active_partition = new
    kind = "REGROW" if len(new) > len(old) else "PROJECT"
    self.lineage.append(LineageEvent(
        kind,
        {"partition": [list(block) for block in new]},
    ))
~~~

- [ ] **Step 5: Verify GREEN**

Run:

~~~bash
python -m unittest tests.test_obstruction tests.test_promotion tests.test_projection -v
~~~

Expected: 8/8 PASS.

- [ ] **Step 6: Commit**

~~~bash
git add runtime/metatron tests/test_projection.py
git commit -m "feat(runtime): add query-relative projection and regrowth"
~~~

### Task 6: Revocation without historical deletion

**Files:** modify runtime/metatron/machine.py; create tests/test_revocation.py

**Produces:** RevokedCapabilityError and Machine.revoke.

- [ ] **Step 1: Write failing test**

~~~python
import unittest

from runtime.metatron.model import STEP_TABLE, State
from runtime.metatron.machine import Machine, RevokedCapabilityError


class RevocationTests(unittest.TestCase):
    def test_revocation_disables_execution_but_preserves_history(self):
        machine = Machine.genesis()
        residual = machine.certify_target(STEP_TABLE)
        candidate = machine.free_extend(residual, "step")
        cert = machine.verify_candidate(candidate, STEP_TABLE)
        machine.promote(candidate, cert)
        relation, relation_cert = machine.verify_relation(("step", "step"), "step")
        machine.install_relation(relation, relation_cert)

        promotion = next(
            event for event in machine.lineage
            if event.kind == "PROMOTE"
        )
        relation_record = machine.relations()

        machine.revoke(cert.id)

        with self.assertRaises(RevokedCapabilityError):
            machine.execute("step", State.ZERO)

        self.assertNotIn("step", machine.active_capability_names())
        self.assertIn(promotion, machine.lineage)
        self.assertEqual(machine.relations(), relation_record)
        self.assertTrue(any(
            event.kind == "REVOKE"
            and event.payload["certificate"] == cert.id
            for event in machine.lineage
        ))


if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run:

~~~bash
python -m unittest tests.test_revocation -v
~~~

Expected: FAIL because RevokedCapabilityError and revoke are missing.

- [ ] **Step 3: Implement revocation**

Add near the existing runtime errors:

~~~python
class RevokedCapabilityError(RuntimeError):
    pass
~~~

Add to Machine:

~~~python
def revoke(self, certificate_id: str) -> None:
    if certificate_id not in self.live_certificates:
        raise ValueError("cannot revoke unknown certificate")
    self.revoked_certificates.add(certificate_id)
    self.lineage.append(LineageEvent(
        "REVOKE",
        {"certificate": certificate_id},
    ))
~~~

Change the revoked branch in execute from ValueError to:

~~~python
if capability.certificate_id in self.revoked_certificates:
    raise RevokedCapabilityError(name)
~~~

Do not delete the capability record, relation record, or any lineage event.

- [ ] **Step 4: Verify GREEN**

Run:

~~~bash
python -m unittest \
  tests.test_obstruction \
  tests.test_promotion \
  tests.test_projection \
  tests.test_revocation -v
~~~

Expected: 9/9 PASS.

- [ ] **Step 5: Commit**

~~~bash
git add runtime/metatron/machine.py tests/test_revocation.py
git commit -m "feat(runtime): make authority revocation history-preserving"
~~~

### Task 7: Deterministic serialization and restart

**Files:** create runtime/metatron/serialization.py and tests/test_serialization.py

**Produces:** dump_machine(machine) -> str and load_machine(payload) -> Machine while preserving the warrant store.

- [ ] **Step 1: Write failing restart test**

~~~python
import unittest

from runtime.metatron.model import Query, STEP_TABLE, State
from runtime.metatron.machine import Machine
from runtime.metatron.serialization import dump_machine, load_machine


class SerializationTests(unittest.TestCase):
    def test_restart_preserves_active_historical_and_warrant_state(self):
        machine = Machine.genesis()
        residual = machine.certify_target(STEP_TABLE)
        candidate = machine.free_extend(residual, "step")
        cert = machine.verify_candidate(candidate, STEP_TABLE)
        machine.promote(candidate, cert)
        relation, rel_cert = machine.verify_relation(("step", "step"), "step")
        machine.install_relation(relation, rel_cert)
        machine.set_queries([Query.IS_ZERO])
        machine.project()

        payload = dump_machine(machine)
        restarted = load_machine(payload)

        self.assertEqual(dump_machine(restarted), payload)
        self.assertEqual(restarted.execute("step", State.ZERO), State.ONE)
        self.assertEqual(restarted.active_partition, ((0,), (1, 2)))
        self.assertEqual(restarted.relations(), ((("step", "step"), "step"),))
        self.assertEqual(restarted.lineage, machine.lineage)
        self.assertEqual(restarted.certificates, machine.certificates)
        self.assertEqual(
            restarted.residual_certificates,
            machine.residual_certificates,
        )
        self.assertEqual(
            restarted._next_residual_serial,
            machine._next_residual_serial,
        )
        self.assertEqual(restarted.live_certificates, machine.live_certificates)
        self.assertEqual(restarted.revoked_certificates, machine.revoked_certificates)


if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run:

~~~bash
python -m unittest tests.test_serialization -v
~~~

Expected: FAIL because runtime.metatron.serialization does not exist.

- [ ] **Step 3: Implement deterministic serialization**

Create runtime/metatron/serialization.py:

~~~python
import json

from .machine import Machine
from .model import (
    Capability,
    Certificate,
    LineageEvent,
    Query,
    Relation,
    ResidualCertificate,
)


def dump_machine(machine: Machine) -> str:
    data = {
        "schema_version": 1,
        "active_partition": [list(block) for block in machine.active_partition],
        "capabilities": {
            name: {
                "table": list(cap.table),
                "certificate_id": cap.certificate_id,
            }
            for name, cap in sorted(machine.capabilities.items())
        },
        "certificates": {
            ident: {
                "kind": cert.kind,
                "subject_digest": cert.subject_digest,
            }
            for ident, cert in sorted(machine.certificates.items())
        },
        "residual_certificates": {
            ident: {
                "target": list(cert.target),
                "target_digest": cert.target_digest,
                "closure_size": cert.closure_size,
                "closure_digest": cert.closure_digest,
                "authority_digest": cert.authority_digest,
            }
            for ident, cert in sorted(machine.residual_certificates.items())
        },
        "next_residual_serial": machine._next_residual_serial,
        "lineage": [
            {"kind": event.kind, "payload": event.payload}
            for event in machine.lineage
        ],
        "live_certificates": sorted(machine.live_certificates),
        "revoked_certificates": sorted(machine.revoked_certificates),
        "queries": [query.value for query in machine.queries],
        "relations": [
            {
                "left": list(relation.left),
                "right": relation.right,
                "certificate_id": cert_id,
            }
            for relation, cert_id in machine._relations
        ],
    }
    return json.dumps(data, sort_keys=True, separators=(",", ":"))


def load_machine(payload: str) -> Machine:
    data = json.loads(payload)
    if data.get("schema_version") != 1:
        raise ValueError("unsupported machine schema")

    machine = Machine()
    machine.active_partition = tuple(tuple(block) for block in data["active_partition"])
    machine.capabilities = {
        name: Capability(
            name=name,
            table=tuple(item["table"]),
            certificate_id=item["certificate_id"],
        )
        for name, item in data["capabilities"].items()
    }
    machine.certificates = {
        ident: Certificate(
            id=ident,
            kind=item["kind"],
            subject_digest=item["subject_digest"],
        )
        for ident, item in data["certificates"].items()
    }
    machine.residual_certificates = {
        ident: ResidualCertificate(
            id=ident,
            target=tuple(item["target"]),
            target_digest=item["target_digest"],
            closure_size=item["closure_size"],
            closure_digest=item["closure_digest"],
            authority_digest=item["authority_digest"],
        )
        for ident, item in data["residual_certificates"].items()
    }
    machine._next_residual_serial = data["next_residual_serial"]
    machine.lineage = [
        LineageEvent(item["kind"], item["payload"])
        for item in data["lineage"]
    ]
    machine.live_certificates = set(data["live_certificates"])
    machine.revoked_certificates = set(data["revoked_certificates"])
    machine.queries = [Query(value) for value in data["queries"]]
    machine._relations = [
        (
            Relation(tuple(item["left"]), item["right"]),
            item["certificate_id"],
        )
        for item in data["relations"]
    ]
    return machine
~~~

- [ ] **Step 4: Verify GREEN**

Run:

~~~bash
python -m unittest discover -s tests -p 'test_*.py' -v
~~~

Expected: all tests present through Task 7 PASS.

- [ ] **Step 5: Commit**

~~~bash
git add runtime/metatron/serialization.py tests/test_serialization.py
git commit -m "feat(runtime): preserve developmental state across restart"
~~~

### Task 8: Lean reference semantics

**Files:** lean-toolchain, lakefile.lean, formal/Metatron/Fixture.lean, formal/Metatron/FixtureTest.lean

**Produces:** reference definitions and finite proofs.

- [ ] **Step 1: Write failing Lean interface test**

~~~lean
import Metatron.Fixture

open Metatron

#check base_cannot_solve_step
#check step_idempotent
#check q0_merges_one_two
#check q1_separates_one_two
#check step_commutes_q0
#check compiled_step_exact
#check revoked_step_denied

example : step State.zero = State.one := rfl
example : projectQ0 State.one = projectQ0 State.two := rfl
example : execInstr Instr.step State.two = State.two := rfl
~~~

- [ ] **Step 2: Add pinned Lean project**

lean-toolchain:

~~~text
leanprover/lean4:v4.35.0-rc2
~~~

lakefile.lean:

~~~lean
import Lake
open Lake DSL

package metatron where
  moreLeanArgs := #["-DautoImplicit=false"]

require mathlib from git
  "https://github.com/leanprover-community/mathlib4.git" @
  "44ba35c6daa9d69aff8fed9fff9bbde17ded774d"

@[default_target]
lean_lib Metatron where
  srcDir := "formal"
  roots := #[`Metatron.Fixture, `Metatron.FixtureTest]

lean_exe metatron_reference where
  srcDir := "formal"
  root := `Metatron.ReferenceCLI
~~~

- [ ] **Step 3: Verify RED**

Run:

~~~bash
lake update
lake exe cache get
lake env lean formal/Metatron/FixtureTest.lean
~~~

Expected: FAIL because Metatron.Fixture is missing.

- [ ] **Step 4: Implement formal/Metatron/Fixture.lean**

~~~lean
namespace Metatron

inductive State
  | zero | one | two
  deriving DecidableEq, Repr

inductive Query
  | isZero | isOne
  deriving DecidableEq, Repr

def idCap : State → State := fun s => s

def step : State → State
  | .zero => .one
  | .one => .one
  | .two => .two

def evalQuery : Query → State → Bool
  | .isZero, .zero => true
  | .isZero, _ => false
  | .isOne, .one => true
  | .isOne, _ => false

theorem base_cannot_solve_step : idCap State.zero ≠ step State.zero := by
  intro h
  cases h

theorem step_idempotent (s : State) : step (step s) = step s := by
  cases s <;> rfl

theorem q0_merges_one_two :
    evalQuery Query.isZero State.one = evalQuery Query.isZero State.two := rfl

theorem q1_separates_one_two :
    evalQuery Query.isOne State.one ≠ evalQuery Query.isOne State.two := by
  decide

inductive Q0Class
  | zero | nonzero
  deriving DecidableEq, Repr

def projectQ0 : State → Q0Class
  | .zero => .zero
  | .one => .nonzero
  | .two => .nonzero

def stepQ0 : Q0Class → Q0Class
  | .zero => .nonzero
  | .nonzero => .nonzero

theorem step_commutes_q0 (s : State) :
    projectQ0 (step s) = stepQ0 (projectQ0 s) := by
  cases s <;> rfl

inductive Instr
  | id | step
  deriving DecidableEq, Repr

def execInstr : Instr → State → State
  | .id => idCap
  | .step => step

theorem compiled_step_exact (s : State) :
    execInstr Instr.step s = step s := rfl

structure AuthoritySnapshot where
  stepLive : Bool
  deriving Repr

def executeStep (Ω : AuthoritySnapshot) (s : State) : Option State :=
  if Ω.stepLive then some (step s) else none

theorem revoked_step_denied (s : State) :
    executeStep ⟨false⟩ s = none := rfl

def stateCode : State → Nat
  | .zero => 0
  | .one => 1
  | .two => 2

def boolCode (b : Bool) : Nat := if b then 1 else 0
def allStates : List State := [.zero, .one, .two]

end Metatron
~~~

- [ ] **Step 5: Verify GREEN and source policy**

~~~bash
mkdir -p .lake/build/lib/lean/Metatron
lake env lean -o .lake/build/lib/lean/Metatron/Fixture.olean formal/Metatron/Fixture.lean
lake env lean formal/Metatron/FixtureTest.lean
! grep -RInE '(^|[^[:alnum:]_])(sorry|admit|axiom|constant|unsafe)([^[:alnum:]_]|$)' formal/Metatron
~~~

Expected: all commands exit 0.

- [ ] **Step 6: Commit**

~~~bash
git add lean-toolchain lakefile.lean formal/Metatron
git commit -m "feat(formal): define Metatron V0 reference semantics"
~~~

### Task 9: Lean/Python differential gate

**Files:** formal/Metatron/ReferenceCLI.lean, tests/test_differential.py

**Produces:** metatron_reference executable and cross-language equality gate.

- [ ] **Step 1: Write failing Python differential test**

~~~python
import subprocess
import unittest

from runtime.metatron.machine import partition_for_queries, project_table
from runtime.metatron.model import Query, STEP_TABLE, State, eval_query


def reference():
    proc = subprocess.run(
        ["lake", "exe", "metatron_reference"],
        check=True,
        capture_output=True,
        text=True,
    )
    out = {}
    for line in proc.stdout.splitlines():
        if "=" in line:
            key, value = line.split("=", 1)
            out[key] = tuple(int(x) for x in value.split(",") if x)
    return out


class DifferentialTests(unittest.TestCase):
    def test_python_matches_lean(self):
        ref = reference()
        self.assertEqual(ref["STEP"], STEP_TABLE)
        self.assertEqual(
            ref["Q0"],
            tuple(int(eval_query(Query.IS_ZERO, s)) for s in State),
        )
        self.assertEqual(
            ref["Q1"],
            tuple(int(eval_query(Query.IS_ONE, s)) for s in State),
        )
        self.assertEqual(ref["Q0_PROJECT"], (0, 1, 1))
        self.assertEqual(
            ref["Q0_STEP"],
            project_table(STEP_TABLE, partition_for_queries([Query.IS_ZERO])),
        )
        self.assertEqual(ref["IDEMPOTENT"], (1,))
        self.assertEqual(ref["COMPILED_EXACT"], (1,))


if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify RED**

Run:

~~~bash
python -m unittest tests.test_differential -v
~~~

Expected: FAIL because metatron_reference cannot build before ReferenceCLI exists.

- [ ] **Step 3: Implement formal/Metatron/ReferenceCLI.lean**

~~~lean
import Metatron.Fixture

namespace Metatron

def renderNatList (xs : List Nat) : String :=
  String.intercalate "," (xs.map toString)

def stepTable : List Nat :=
  allStates.map (fun s => stateCode (step s))

def q0Table : List Nat :=
  allStates.map (fun s => boolCode (evalQuery Query.isZero s))

def q1Table : List Nat :=
  allStates.map (fun s => boolCode (evalQuery Query.isOne s))

def q0ProjectCode : Q0Class → Nat
  | .zero => 0
  | .nonzero => 1

def q0ProjectTable : List Nat :=
  allStates.map (fun s => q0ProjectCode (projectQ0 s))

def q0StepTable : List Nat :=
  [Q0Class.zero, Q0Class.nonzero].map (fun c =>
    q0ProjectCode (stepQ0 c))

def idempotentFlag : Nat :=
  boolCode (allStates.all (fun s => decide (step (step s) = step s)))

def compiledExactFlag : Nat :=
  boolCode (allStates.all (fun s =>
    decide (execInstr Instr.step s = step s)))

def main : IO Unit := do
  IO.println s!"STEP={renderNatList stepTable}"
  IO.println s!"Q0={renderNatList q0Table}"
  IO.println s!"Q1={renderNatList q1Table}"
  IO.println s!"Q0_PROJECT={renderNatList q0ProjectTable}"
  IO.println s!"Q0_STEP={renderNatList q0StepTable}"
  IO.println s!"IDEMPOTENT={idempotentFlag}"
  IO.println s!"COMPILED_EXACT={compiledExactFlag}"

end Metatron
~~~

- [ ] **Step 4: Verify GREEN**

Run:

~~~bash
lake build metatron_reference
python -m unittest tests.test_differential -v
~~~

Expected: 1/1 PASS.

- [ ] **Step 5: Commit**

~~~bash
git add formal/Metatron/ReferenceCLI.lean tests/test_differential.py
git commit -m "test: compare Python runtime with Lean reference semantics"
~~~

### Task 10: One-loop capstone

**Files:** tests/test_capstone.py

**Produces:** complete lifecycle gate.

- [ ] **Step 1: Write capstone test**

~~~python
import unittest

from runtime.metatron.model import Query, STEP_TABLE, State, eval_query
from runtime.metatron.machine import Machine, RevokedCapabilityError
from runtime.metatron.serialization import dump_machine, load_machine


class CapstoneTests(unittest.TestCase):
    def test_complete_loop(self):
        machine = Machine.genesis()

        residual = machine.certify_target(STEP_TABLE)
        self.assertIsNotNone(residual)
        self.assertEqual(residual.closure_size, 1)

        candidate = machine.free_extend(residual, "step")
        cert = machine.verify_candidate(candidate, STEP_TABLE)
        machine.promote(candidate, cert)
        self.assertEqual(machine.execute("step", State.ZERO), State.ONE)

        relation, rel_cert = machine.verify_relation(("step", "step"), "step")
        machine.install_relation(relation, rel_cert)
        self.assertEqual(machine.relations(), ((("step", "step"), "step"),))

        machine.set_queries([Query.IS_ZERO])
        machine.project()
        self.assertEqual(machine.active_partition, ((0,), (1, 2)))

        for state in State:
            full = eval_query(Query.IS_ZERO, machine.execute("step", state))
            source_class = next(
                i for i, block in enumerate(machine.active_partition)
                if int(state) in block
            )
            quotient_target = (1, 1)[source_class]
            self.assertEqual(full, quotient_target == 0)

        payload = dump_machine(machine)
        restarted = load_machine(payload)
        self.assertEqual(dump_machine(restarted), payload)
        self.assertEqual(restarted.execute("step", State.ZERO), State.ONE)

        promotion = next(e for e in restarted.lineage if e.kind == "PROMOTE")
        restarted.revoke(cert.id)
        with self.assertRaises(RevokedCapabilityError):
            restarted.execute("step", State.ZERO)
        self.assertIn(promotion, restarted.lineage)

        prefix = tuple(restarted.lineage)
        restarted.set_queries([Query.IS_ZERO, Query.IS_ONE])
        restarted.project()
        self.assertEqual(restarted.active_partition, ((0,), (1,), (2,)))
        self.assertEqual(tuple(restarted.lineage[:len(prefix)]), prefix)


if __name__ == "__main__":
    unittest.main()
~~~

- [ ] **Step 2: Verify**

~~~bash
python -m unittest tests.test_capstone -v
lake build metatron_reference
python -m unittest discover -s tests -p 'test_*.py' -v
~~~

Expected: every test PASS.

- [ ] **Step 3: Commit**

~~~bash
git add tests/test_capstone.py
git commit -m "test: qualify complete Metatron V0 lifecycle"
~~~

### Task 11: Dedicated qualification workflow

**Files:** .github/workflows/metatron-v0.yml

**Produces:** exact-head CI gate.

- [ ] **Step 1: Create workflow**

~~~yaml
name: Metatron V0

on:
  push:
    branches: [main, metatron-v0]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  v0:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate evidence manifest
        run: |
          python scripts/check_manifest.py
          python -m unittest tests.test_manifest -v

      - name: Install Lean
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Fetch pinned Mathlib
        run: |
          lake update
          lake exe cache get

      - name: Reject local proof placeholders
        run: |
          set -euo pipefail
          ! grep -RInE '(^|[^[:alnum:]_])(sorry|admit|axiom|constant|unsafe)([^[:alnum:]_]|$)' formal/Metatron

      - name: Build Lean reference semantics
        run: |
          lake build Metatron
          lake build metatron_reference
          lake env lean formal/Metatron/FixtureTest.lean

      - name: Run Python and differential tests
        run: python -m unittest discover -s tests -p 'test_*.py' -v
~~~

- [ ] **Step 2: Verify exact workflow commands locally**

Run manifest checker, both Lake builds, FixtureTest, placeholder scan, and full unittest discovery.

Expected: all exit 0.

- [ ] **Step 3: Commit**

~~~bash
git add .github/workflows/metatron-v0.yml
git commit -m "ci: add Metatron V0 qualification gate"
~~~

### Task 12: Remote qualification and evidence closure

**Files:** create evidence/qualified-runs/metatron-v0.json; modify README.md and spec/CLAIM_BOUNDARY.md

**Produces:** exact-head qualification record.

- [ ] **Step 1: Push execution branch**

Run:

~~~bash
git push -u origin metatron-v0
~~~

Use GitHub Actions to find the Metatron V0 run whose head_sha exactly equals:

~~~bash
git rev-parse HEAD
~~~

Expected: status completed, conclusion success.

- [ ] **Step 2: Verify required remote steps**

The exact run must show success for:

~~~text
Validate evidence manifest
Fetch pinned Mathlib
Reject local proof placeholders
Build Lean reference semantics
Run Python and differential tests
~~~

A missing, skipped, cancelled, or failed required step means V0 is not qualified.

- [ ] **Step 3: Create qualification record from observed run values**

After obtaining RUN_ID, HEAD_SHA, and RUN_URL from the successful run, execute:

~~~bash
python - "$RUN_ID" "$HEAD_SHA" "$RUN_URL" <<'PY'
import json
import sys
from pathlib import Path

run_id = int(sys.argv[1])
head_sha = sys.argv[2]
run_url = sys.argv[3]

record = {
    "schema_version": 1,
    "name": "Metatron V0",
    "status": "QUALIFIED",
    "head_sha": head_sha,
    "workflow_run": {
        "id": run_id,
        "conclusion": "success",
        "url": run_url,
    },
    "claim": (
        "Finite proof-carrying developmental lifecycle: exact obstruction, "
        "verified capability promotion, earned relation, query-relative "
        "compression, deterministic restart, revocation, and regrowth."
    ),
    "excluded_claims": [
        "GeneratedCongruence(Omega_earned) = PathBehEq",
        "residualAdjoin_strictLyapunov",
        "unrestricted self-improvement",
        "infinite productivity"
    ]
}

path = Path("evidence/qualified-runs/metatron-v0.json")
path.parent.mkdir(parents=True, exist_ok=True)
path.write_text(json.dumps(record, indent=2, sort_keys=True) + "\n")
PY
~~~

- [ ] **Step 4: Update public claim status**

Append to README.md:

~~~markdown
## Qualification

Metatron V0 has a qualified finite lifecycle at the exact head recorded in
evidence/qualified-runs/metatron-v0.json.

The qualification is intentionally bounded. General earned-equation
completeness and the strict Lyapunov bridge remain outside V0.
~~~

In spec/CLAIM_BOUNDARY.md change only:

~~~text
## Intended V0 qualified claim
~~~

to:

~~~text
## V0 qualified claim
~~~

Do not alter excluded claims.

- [ ] **Step 5: Commit evidence closure**

~~~bash
git add evidence/qualified-runs/metatron-v0.json README.md spec/CLAIM_BOUNDARY.md
git commit -m "evidence: record qualified Metatron V0 lifecycle"
~~~

- [ ] **Step 6: Re-run complete local qualification on the evidence commit**

Run:

~~~bash
python scripts/check_manifest.py
lake build Metatron
lake build metatron_reference
lake env lean formal/Metatron/FixtureTest.lean
! grep -RInE '(^|[^[:alnum:]_])(sorry|admit|axiom|constant|unsafe)([^[:alnum:]_]|$)' formal/Metatron
python -m unittest discover -s tests -p 'test_*.py' -v
~~~

Expected: all commands exit 0.

- [ ] **Step 7: Push and require one final exact-head green run**

Run:

~~~bash
git push
~~~

The final user report must cite the Metatron V0 run on this evidence commit, not the earlier pre-evidence run.

## Self-Review Checklist

- Every approved spec section is either implemented here or explicitly excluded from V0.
- The three-state fixture is the only V0 world.
- ID_TABLE, STEP_TABLE, query names, relation, and partition ordering are consistent across runtime, Lean, tests, and docs.
- Nucleus remains PENDING.
- The only earned relation is step ; step = step.
- No completeness claim is inferred from that one relation.
- Insufficiency is exhaustive closure failure, never timeout/budget failure.
- Unsafe quotienting fails closed.
- Revocation preserves history.
- Serialization covers every mutable Machine field.
- Differential testing executes Lean definitions.
- Qualification is not claimed before a fresh exact-head green workflow.
